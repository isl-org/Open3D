name: Ubuntu ARM64 CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:
    types: [opened, reopened, synchronize]  # Rebuild on new pushes to PR

jobs:
  ubuntu-arm64:
    runs-on: ubuntu-18.04
    strategy:
      fail-fast: false
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2
        with:
          submodules: true
      - uses: uraimo/run-on-arch-action@v2.0.2
        name: Run commands
        id: runcmd
        with:
          arch: aarch64
          distro: ubuntu18.04
          githubToken: ${{ github.token }} # Not required, but speeds up builds
          dockerRunArgs: |
            --volume "${PWD}:/Open3D"
          install: |
            apt-get update -q -y
            apt-get install -q -y git python python3 python3-dev python3-pip
          run: |
            uname -a
            python --version || true
            python3 --version || true
            which pip || true
            which pip3 || true
            pip3 --version || true
            cd Open3D || true
            git submodule update --init --recursive || true
            git status || true
            git branch -vv || true
            ./util/install_deps_ubuntu.sh assume-yes || true
