# liblzf is only used in IO. It is thus included in IO.
# If you want to use liblzf as an independent library, uncomment the following
# lines.
#
#project(liblzf)
#add_library(liblzf STATIC lzf_c.c lzf_d.c lzf.h)
